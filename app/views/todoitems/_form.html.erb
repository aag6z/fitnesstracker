<%= form_for(@todoitem) do |f| %>
  <% if @todoitem.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@todoitem.errors.count, "error") %> prohibited this todoitem from being saved:</h2>

      <ul>
      <% @todoitem.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
<div class="panel panel-default">
  <div class="panel-body">
   
  <div class="form-group">
    <%= f.label :title, "Task Title" %><br>
    <%= f.text_field :title %>
  </div>
  <div class="form-group">
    <%= f.label :task, "Task Description" %><br>
    <%= f.text_field :task, size: "60x12" %>
  </div>
  <div class="actions">
    <%= f.submit "Enter", class: "btn btn-primary"%>
  </div>
<% end %>
 
  </div>
</div>